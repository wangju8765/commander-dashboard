# 主宰面板 · 运行日志

## 2026-05-29 — v0.8.4 添加 SVG 图标（favicon + iOS 主屏幕）

### 变更内容
- 新增 `icon.svg` — 深色圆角方形图标，金色皇冠 + 红色龙虾钳 + 紫色宝石
- `index.html` `<head>` 加了三行：`rel=icon`、`rel=apple-touch-icon`、`rel=mask-icon`
- 版本号 v0.8.4
- 在线部署：✅ 已推送（380d99a），等待 GitHub Pages 刷新

### 讨论记录
- **项目看板自动同步机制：** 讨论 Cron 任务方案，最终决定先放一放，下次用户直接告诉我更新
- **「库」标签页现状：** entries 表 0 条记录，三个分类（思考/行动/规划）从未被使用过，需用户决定保留还是改造

## 2026-05-28 晚上session — v0.8.2 方向卡片修复

### 代码变更
- v0.8.1: 方向CSS从桌面媒体查询块移出到全局作用域（子agent修复）
- v0.8.2: 方向Tab 6项修复（子agent修复，未推送）

### 方向页面6项修复（v0.8.2）

1. **删除底部标签条** — 去掉 `#dirChips` 及 chips 相关全部代码
2. **Snap锁定修复** — `#tab-goals` 改为 `flex` 容器，卡片 `flex:1` 撑满全屏
3. **卡片边框/底色加强** — tint 配色更明显
4. **卡片高度撑满全屏** — `dir-card` + `dir-page` 双 flex
5. **卡片标题居中** — `.dir-card-label { text-align: center }`
6. **清理** — `shortName()` 孤立函数已删除

### 技术教训
- 方向CSS放在 `@media(min-width:768px)` 内导致手机端样式全失效
- `tab-content` 是 `display:block`，内部 flex 子元素的 `height:100%` 无效，必须父容器也 flex
- 本地无法推送 GitHub（443 timeout），需手动 push

### v0.8.2 六项修复（已推送）
- v0.8.3 （已推送）：卡片宽度修复 `min-width:100vw` → `flex:0 0 100%`

## 2026-05-28 下午session

### v0.8.0 方向与聚焦（全面替换目标板块）

### v0.8.0 方向与聚焦（全面替换目标板块）

#### 讨论
- 用户不想要传统「目标追踪」（进度/截止日期），改为方向与聚焦
- 从12版mockup迭代后，用户选定v9（星空+星系）风格
- 最终混合版：北极星「⭐ 生活即工作」+4张snap锁定滑动卡片
- 卡片分类：产品/AI数字化/内容/家庭，各有主题色边框tint
- 所有文字≥20px全粗体，无需灰色劣化
- 方向项由AI维护，用户只读，无新增/编辑/删除入口

#### 代码变更
- v0.8.0: 方向与聚焦板块实现
  - 替换旧的 goals 表/视图为 directions 表
  - 手机版：snap横向滑动4卡片 + 小圆点指示器 + 底部活跃项标签
  - 电脑版：4列卡片网格布局
  - 点击项目弹出tooltip显示状态
  - 北极星独立渲染
  - 去掉了新建/编辑目标的表单入口
  - 旧CSS清理

#### 数据库
- 新建 directions 表（north/product/ai/content/family）
- 种子数据：11条（1北极星+10个方向项）
- 建表SQL: setup-directions.sql

#### BUG 修复
- 新表 directions 缺少 RLS 权限 → 匿名 key 返回空数组，页面看不到卡片
- 修复：ALTER TABLE + CREATE POLICY（setup-directions-rls.sql / 已合并入 setup-directions.sql）

#### 技术教训
- Supabase 建表需通过 Dashboard SQL 编辑器执行，无法通过 REST API 或命令行直接操作
- 新表必须立刻加 RLS 策略（CREATE POLICY），否则浏览器端 anon key 读不到数据

## 2026-05-27 晚上session（延续）

### v0.7.4-v0.7.8 连续迭代

#### 讨论
- 用户发现电脑版日历缺少手机版的 time-bar 和 past 样式，需对齐
- 因之前回滚到v0.7.0引发用户不满，恢复v0.7.4全功能

#### 代码变更
- **v0.7.4**: 电脑日历 day-pill 加 time-bar + 事件列表 past 样式（erickson）
- **v0.7.5**: iOS 滚动归零修复 — scrollTop 改到DOM渲染后执行+scrollTo双保险+setTimeout fallback
- **v0.7.6**: 手机版/桌面版CSS互相隐藏，解决页面加载时闪烁
- **v0.7.7**: 项目详情页大改 — 结构化简介卡片（背景/目标/进展）+ 项目产出取代文档，支持链接型产出
- **v0.7.8**: 项目列表卡片描述截断为60字

#### 数据库
- 7个项目的description更新为三段式Markdown格式
- 主宰面板、速算24点游戏 新增 link 型产出

#### 技术教训
- iOS `-webkit-overflow-scrolling: touch` 下 scrollTop 改在 DOM 渲染后才会生效
- 两套视图必须 CSS 层就互相隐藏，不能只靠 JS
- anon key 可以用 PATCH（RLS Allow all），service_role key 可能需要单独授权

## 2026-05-27 全天session

### 讨论

### 讨论
- 项目定义明确：需跟踪状态的任务即项目，4个生命周期
- 文档方案：Markdown 内容存 docs JSONB 字段，链接跳转阅读
- 更新机制：每天12:00和24:00同步，绑定心跳和日报
- 工作模式变更：技术执行交 subagent（reasoner），主宰只拆任务+验收+汇报
- 用户确认：不要编辑/删除按钮（只读）、信息行隐藏、返回按钮固定顶部

### 代码变更（4个版本）
- v0.7.0: 文档折叠阅读 + 数据库加字段
- v0.7.1: 项目详情弹窗→独立页面 + 文档独立阅读页
- v0.7.2: 去编辑/删除、隐藏信息行、已完成按agent分组、返回按钮固定顶部、mdToHtml增强
- v0.7.3: 表格分隔符跳过、代码块间距、scrollTop重置

### 数据库
- projects 表加 docs JSONB
- 引光小站调研+天才小熊猫调研 两个独立已完成项目（主宰）
- 删除合并的「公众号调研」
- 更新所有项目进度/描述

### 技术教训
- 用 subagent + reasoner 模型做代码修改效果好（只需我验证+上线，不占对话上下文）
- 三个 subagent 并行修改同一文件要注意不要冲突——当前是每个改不同函数/CSS区域，不重叠
- 共享滚动容器切换页面时必须重置 scrollTop，否则长文档返回后页面空白
- mdToHtml 渲染顺序很重要：代码块必须先处理用占位符替换，再处理其他元素

### 补记：文档阅读体验升级（同一会话尾声）

#### 讨论
- 用户指出所有报告的阅读体验极差：MD文档左右滚动、HTML提取文本乱码
- 提出四个方案（修CSS/部署链接/全屏阅读/新标签页），用户选择修CSS+部署链接混合方案
- 被批评第一次HTML转换只是机械转格式、没有真正设计；第二轮重新设计了完整信息层次

#### 变更
- v0.7.9: 去掉列表摘要、只留标题
- 《神经功能训练·全家手册.html》部署到 neuro-training 仓库 GitHub Pages
- 引光小站调研、天才小熊猫调研 MD内容转为独立HTML页面部署到 commander-dashboard
- 两套HTML经过重新设计：卡片布局/时间线/对比格/信息层次/重点色
- 每日打卡项目：名称/状态/进度/lead/description/产出全部更新
- 所有项目的产出统一为link类型，删除内嵌MD内容

#### 技术教训
- HTML文档要用心设计，不只是转格式——排版、配色、卡片、信息层级缺一不可
- MD文件可以通过GitHub blob链接直接渲染（不需要额外部署）

## 2026-05-28 下午session

### 目标页面设计讨论（12版mockup迭代）

#### 需求理解
- 用户不是目标驱动型而是兴趣驱动型 → 放弃传统「目标追踪」，改为「方向与聚焦」
- 去掉了进度百分比和截止日期
- 引入「星系」概念：用户做的所有事归入不同领域

#### 从星图到卡片（v1→v12）
- v1: 初始星空概念示意图（星座比喻）
- v2-v7: 自由布局星图探索（浮动、星系、星云、坐标轴）
- v8-v9: 回到规整布局 + 横向滑动卡片
- v10-v11: 全屏滑动4卡片 + 分页小圆点
- v12: 最终版 — 简化标题、去分隔文字、活跃项蓝色框区分

#### 最终设计决策
- 大标题：✦ 生活即工作
- 4张滑动卡片：产品 / AI数字化 / 内容 / 家庭
- 无「当前聚焦」「已完成」文字标题
- 活跃项：蓝色框底（rgba蓝底+蓝边框），18px纯白
- 已完成项：无框，16px浅灰，正常可读
- 分页dots：蓝色长条指示当前页

#### 文件
- `mockup-goals-v12.html` — 最终版
- 前11版保留在项目目录中供参考
